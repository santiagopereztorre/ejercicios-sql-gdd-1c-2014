-- Mostrar para todos los rubros de artículos código, detalle, cantidad de artículos de 
-- ese rubro y stock total de ese rubro de artículos. Solo tener en cuenta aquellos 
-- artículos que tengan un stock mayor al del artículo ‘00000000’ en el depósito ‘00’. SELECT rubr_id, rubr_detalle, COUNT(prod_codigo), SUM(stoc_cantidad)FROM dbo.Rubro	INNER JOIN dbo.Producto ON rubr_id = prod_rubro	INNER JOIN dbo.STOCK ON prod_codigo = stoc_productoGROUP BY rubr_id, rubr_detalleHAVING SUM(stoc_cantidad) > (SELECT SUM(stoc_cantidad) FROM dbo.STOCK WHERE stoc_producto = '00000000')